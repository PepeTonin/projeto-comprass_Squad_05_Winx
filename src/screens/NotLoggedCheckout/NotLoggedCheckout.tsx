import { Text, View } from 'react-native';

import { styles } from './style';

export default function NotLoggedCheckout() {
  return (
    <View style={styles.container}>
      <Text>Not logged checkout screen</Text>
    </View>
  );
}